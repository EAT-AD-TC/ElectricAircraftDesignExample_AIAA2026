# Core scientific stack used by the notebooks and helper modules
numpy
pandas
matplotlib
ipython

# Notebook/kernel support for running the worked examples
jupyterlab
ipykernel

# Test dependency used in tests/
pytest

# Note:
# - OpenVSP is referenced by the builder notebook/tooling, but it is not a
#   pip dependency here. Install the OpenVSP application separately if you
#   want to use Jetstream31_OpenVSP_Builder.ipynb.
